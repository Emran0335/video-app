import { Video } from "@/state/api";
import React from "react";

type DashboardVideoCardProps = {
  video: Video;
};

const DashboardVideoCard = ({ video }: DashboardVideoCardProps) => {
  return <div>DashboardVideoCard</div>;
};

export default DashboardVideoCard;
